package ${package}.${moduleName}.service;

import ${package}.${moduleName}.entity.${className};
import cn.com.hatech.common.data.universal.IBaseService;
import com.github.pagehelper.PageInfo;
import java.util.Map;

/**
 * @Copyright (C), 2018,北京同创永益科技发展有限公司
 * @ProjectName: hatech
 * @Package:  ${package}.${moduleName}.service
 * @ClassName: I${className}Service
 * @Author: ${author}
 * @Description: ${moduleChineseName}业务接口
 * @Date: ${datetime}
 * @Version: 1.0
 */
public interface I${className}Service extends IBaseService<${className}> {
    /**
    * 根据条件分页查询${moduleChineseName}信息
    *
    * @param map
    * @return
    */
    PageInfo<${className}> findAll(Map<String, Object> map);

    /**
     * 编辑${moduleChineseName}信息
     *
     * @param map
     * @return
     */
    int edit(Map<String, Object> map);

    /**
     * 根据ids批量删除${moduleChineseName}信息
     *
     * @param ids
     * @return
     */
    int delete(String ids);
}

